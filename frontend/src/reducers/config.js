let defaultState = {
    popUp: {
        defaultDict: 2,
        limit: 5
    }
};

const config = (state = defaultState, action) => {
    switch (action.type) {
        default:
            return state
    }
};

export default config;